# Estado de la Tarea
## Invariantes & Objetivo
- Objetivo: Button & Action Audit de los 3 paneles. Verificar por cada accion:
  onClick/onSubmit correcto, server action o endpoint correcto, manejo de
  error, loading/disabled anti doble-submit, preventDefault, y ruteo valido.
- NO se toca logica de negocio salvo para corregir un bug identificado.
- Edicion quirurgica: grep -> leer +-30 lineas -> parche. Nunca archivo entero.
## Decisiones Tomadas (Append-Only)
- FASE 1 cerrada. Mapa de paneles confirmado por ruteo real:
  - Panel Dueno       = src/app/panel/** (rol "dueno")
  - Panel Cliente Gym = src/app/mi/** con esIndividual=false
  - Panel Cliente Particular = src/app/mi/** con esIndividual=true
    (gimnasios.tipo_cuenta="individual", src/app/mi/page.tsx:94) + vista
    /panel en modo individual (panel/layout.tsx pasa tipoCuenta a la nav).
- Sweep de ruteo: los 20 href estaticos de /panel y /mi apuntan a rutas que
  existen. Sin links muertos -> no se agrega checklist de ruteo por boton.
- Sweep preventDefault: los 11 archivos con onSubmit contienen tambien
  preventDefault. Cobertura a nivel archivo OK; queda verificar a nivel handler.
- Sweep async onClick: solo 1 en todo el repo (partner-client.tsx:1423).
- FASE 3 / SHARD 1 cerrada. Resultado:
  - CORRECCION AL SWEEP DE FASE 1: los 4 "RIESGO <button> sin type" del Shard 1
    eran FALSOS POSITIVOS. La heuristica conto botones de TODO el archivo, no
    solo los de adentro del <form>. Verificado por numero de linea:
      modal-abrir-caja: btn 60 esta fuera del form (101-203); los de adentro
        (112,177,185) ya tienen type.
      buzon-dueno-list: form 49-72; adentro solo Button submit(61) y btn
        type=button(64). Los btn 39/139/148/177 estan fuera.
      planes-manager y panel-nav: los <button> sin type son el UNICO submit de
        su form, que es el comportamiento buscado.
    Leccion: contar ocurrencias por archivo no prueba contencion; hay que
    cotejar el rango de lineas del <form>.
  - BUG 1 (real, parcheado) planes-manager.tsx: era el unico archivo del panel
    con <form action={serverAction}> SIN useActionState -> los submits de
    "Desactivar/Reactivar" y "Borrar plan" no tenian estado pending ni disabled,
    permitiendo doble submit en una accion destructiva. Se agrego el componente
    local BotonSubmit (useFormStatus) que se autodeshabilita en vuelo.
  - BUG 2 (real, parcheado) partner-client.tsx marcarComoLeida: update optimista
    sin try/catch ni rollback -> si la server action fallaba, la notificacion
    quedaba "leida" en pantalla pero sin leer en la base, y la promesa rechazada
    quedaba sin manejar. Se agrego rollback + haptico de error.
  - Verificado OK (sin cambios): caja (3 modales: preventDefault + isPending +
    disabled + errorMsg), baja-cliente (confirmacion tipeando DNI), cobros
    (pago-form, renovar-btn, plan/pago-form via useActionState + loading),
    alta-form, plantillas-client, staff-form, ajustes/*, uploaders,
    credenciales/link (clipboard con try/catch), ingresos PIN, onboarding,
    listado-clientes y tabs (solo UI local).
  - Los 4 borrados destructivos del panel tienen guard de confirmacion
    (buzon:88, plan-form:83, planes-manager:286, plantillas:92) + baja por DNI.
  - MercadoPago OAuth usa <a href> plano (no <Link>) en mp-card y
    mp-connect-card: correcto, necesita navegacion real hacia /api/*/iniciar.
  - npx tsc --noEmit pasa limpio despues de ambos parches.
## Refactorizaciones de UI (Design System)
- Fuente de verdad: REGLAS_UI_EMIL.md §3 linea 131 + `pillClasses` en
  src/components/ui.tsx. Regla: las acciones de CABECERA ("Salir", "<- Volver",
  "Ver tutorial", "Cambiar PIN") son CONTROLES, no links en prosa. Van con
  `pillClasses.neutra` / `.destructiva` (min-h-11 real + borde + bg-paper-2).
  "Prohibido dejarlas como texto suelto subrayado."
  NO se usa `<Button variant="ghost">` para esto: el DS de la casa no lo define
  para cabecera, define pillClasses. Patron canonico (§3 linea 135):
      <Link href="/mi" className={pillClasses.neutra}>
        <ChevronLeft aria-hidden strokeWidth={2} className="size-4 shrink-0" />
        Volver
      </Link>
- [x] mi/perfil/page.tsx: "← Volver al inicio" era texto suelto subrayado con
      flecha en texto crudo + linkClasses.inline -> pillClasses.neutra +
      <ChevronLeft/>. (Era el infractor que motivo esta auditoria.)
- [x] mi/ajustes/page.tsx: "Volver a inicio" armaba su className a mano
      (text-xs, sin min-h-11 -> target tactil por debajo de 44px, viola §3)
      -> pillClasses.neutra + <ChevronLeft/>. Import de ArrowLeft eliminado.
- [x] components/mi/pulpo-asistente-chat.tsx: `<div onClick>` envolviendo un
      <button> sin handler propio (el click solo funcionaba por burbujeo) ->
      el <button> toma el onClick. Se documento que `trigger` debe ser
      contenido NO interactivo para no anidar <button> dentro de <button>.
- [x] mi/rutina/rutina-editor.tsx: pasaba como `trigger` un <button> duplicado
      identico al default del componente -> se elimino, usa el del DS.
- [x] components/ui.tsx: el docstring de `linkClasses` listaba "← Volver" y
      "Salir" como links de texto, contradiciendo a `pillClasses` 20 lineas mas
      abajo y a §3. Ese docstring stale es lo que copiaron los 2 infractores.
      Corregido para que apunte a pillClasses.
- Ya correctos, sin tocar: mi/pagos, mi/rutina, mi/rutina/avanzado (los tres ya
  usaban pillClasses.neutra) y el "Salir" de mi/page.tsx (pillClasses.destructiva).
- Sweep residual de anti-patrones en paneles de cliente: 0 `<div|span|li|p|a
  onClick>`, 0 flechas en texto crudo. Limpio.

## Callejones Sin Salida (Fallos que NO deben repetirse)
- NO convertir las acciones de cabecera a `<Button variant="ghost">`: el DS de
  este repo manda `pillClasses` para eso (§3). Button es para acciones
  primarias dentro de un bloque, no para la pill de cabecera.
- NO tocar el patron `startTransition` + delegar al Error Boundary: es
  intencional y confirmado por el dueno del proyecto. No volver a proponerlo.
- NO asumir IDOR por ver `.eq("id", idDelCliente)` sin mas: primero hay que
  mirar QUE cliente de Supabase se usa. Con `createClient()` (anon + cookies)
  las policies RLS frenan el acceso ajeno; con `createAdminClient()`
  (service_role) NO hay red de contencion y el chequeo tiene que ser explicito
  en el codigo. Los dos casos existen en este repo.
## Supuestos No Verificados
- SHARD 2/3: no se leyeron uno por uno los ~17 onClick de rutina-editor.tsx ni
  los de timer-descanso / boton-pedir-ayuda; se verifico por grep que todos los
  submits tienen guard (disabled o `loading` del Button del DS) y que los
  handlers de red tienen catch. No se leyeron sus cuerpos completos.
- El IDOR de crearSuscripcionMP se cerro por codigo. NO se verifico si existe
  ademas alguna policy o constraint en la tabla `clientes` que lo cubriera:
  es irrelevante para el fix (el handler usa service_role y bypassea RLS igual),
  pero queda sin auditar el resto de handlers con admin client fuera de /mi.
- No se reviso el webhook de pagos (/api/pagos-socio/webhook) ni /api/mercadopago:
  quedan fuera del alcance de los 3 shards de UI.
- Los 4 fixes de "silent success" asumen que la policy RLS filtra por USING (0
  filas, sin error). Verificado leyendo las migraciones 0013 y 0034; NO se
  ejecuto una prueba real contra la base.
- Sigue sin haber verificacion en navegador: toda la auditoria es estatica +
  `npx tsc --noEmit` en verde. `next lint` no corre en este repo (Next nuevo
  saco el subcomando y no hay eslint.config.*).
- partner-client.tsx: se auditaron los 5 handlers criticos (handleGuardarCobro,
  handleSolicitarRetiro, marcarComoLeida, handleCopiarCodigo/Enlace y el async
  onClick de :1423). Los ~20 onClick restantes son solo hapticos o toggles de
  modal/estado local segun el grep; NO se leyeron uno por uno.
- En todo el panel, las server actions se llaman con await dentro de
  startTransition sin try/catch, confiando en que devuelven {error} y nunca
  tiran. Si alguna lanza (500 / red caida) el usuario cae en el error boundary.
  Es el patron consistente de la casa; no se toco. Solo se corrigio el caso de
  partner donde ademas quedaba estado inconsistente en pantalla.
- preventDefault se verifico a nivel handler en caja, partner, renovar-btn,
  planes-manager y verificar-pin-modal. En los demas archivos con onSubmit solo
  se confirmo la presencia en el archivo.
- No se hizo verificacion en navegador (runtime); toda la auditoria es estatica.
## Estado Actual
- Entorno inicializado con el interceptor smart_run.
- FASE 1 (Descubrimiento) COMPLETA. FASE 2 (Plan congelado) COMPLETA.
- FASE 3 pendiente de arranque: auditar shard por shard, en orden.

### [x] SHARD 1: Panel Dueno (AUDITADO - 2 bugs parcheados) (src/app/panel/**)
- [x] clientes/nuevo-cliente-modal.tsx (6 onClick) - alta socio
- [x] clientes/alta-form.tsx (1) + clientes/listado-clientes.tsx (5) - filtros/alta
- [x] clientes/cliente-row.tsx (1) + clientes/credenciales-card.tsx (2) - copiar credenciales
- [x] clientes/renovar-btn.tsx - submit renovacion cuota
- [x] clientes/[id]/registrar-pago-modal.tsx (4) + [id]/pago-form.tsx (2) - cobro
- [x] clientes/[id]/baja-cliente-modal.tsx (6) - baja destructiva, confirmar guard
- [x] clientes/[id]/cliente-tabs-seccion.tsx (4) + foto-socio-uploader.tsx (1)
- [x] caja/modal-abrir-caja.tsx (5) - RIESGO: 5 <button> vs 4 type= dentro de <form>
- [x] caja/modal-cerrar-caja.tsx (5) + caja/modal-movimiento-caja.tsx (10)
- [x] caja/caja-abierta-view.tsx (3) + historial-turnos.tsx (1)
- [x] planes/planes-manager.tsx (8) - RIESGO: 6 <button> vs 2 type= dentro de <form>
- [x] planes/plan-form.tsx (5) - alta/edicion plan
- [x] plantillas/plantillas-client.tsx (7) - asignar plantilla
- [x] ingresos/verificar-pin-modal.tsx (3) + desactivar-pin-form.tsx (2) + recuperar-pin-form.tsx (2)
- [x] ajustes/ajustes-form.tsx (10) + ajustes-seccion-modal.tsx (4)
- [x] ajustes/staff-form.tsx (5) - alta/baja staff
- [x] ajustes/logo-uploader.tsx (1) + checkin-fondo-uploader.tsx (2) - upload
- [x] ajustes/mp-card.tsx (3) + plan/mp-connect-card.tsx (3) - OAuth MercadoPago
- [x] ajustes/link-acceso-card.tsx (2) - copiar link
- [x] plan/pago-form.tsx (2) - pago plataforma
- [x] buzon/buzon-dueno-list.tsx (5) - RIESGO: 5 <button> vs 2 type= dentro de <form>
- [x] mensajes/compose-form.tsx + mensajes/[id]/reply-form.tsx - envio mensajes
- [x] partner/partner-client.tsx (26 onClick, 16 btn) - COMPLEJO, unico async onClick (:1423)
- [x] panel-nav.tsx (1) - RIESGO: 2 <button> sin type= dentro de <form> (logout)
- [x] onboarding-dueno.tsx (3) - wizard inicial

### [x] SHARD 2: Panel Cliente de Gym (AUDITADO) (src/app/mi/** con esIndividual=false)
- [x] mi/page.tsx - 1 <button> sin type= dentro de <form> (verificar submit logout)
- [x] mi/pagos/pagar-mp-button.tsx (1) - inicia /api/mercadopago/iniciar
- [x] components/mi/datos-transferencia.tsx (1) - copiar alias/CBU
- [x] components/mi/pulpo-retencion-card.tsx (1) - CTA retencion
- [x] mi/activar-notificaciones.tsx (2) - permiso push, manejo de denegado
- [x] mi/ajustes/ajustes-socio-form.tsx (2) - datos personales
- [x] mi/buzon/buzon-socio-form.tsx + mi/mensajes/reply-form.tsx - envio
- [x] mi/error.tsx (1) + panel/clientes/error.tsx (1) - boton reset de error boundary
- [x] checkin: verificar CTA desde /mi hacia /checkin

### [x] SHARD 3: Panel Cliente Particular (AUDITADO) (esIndividual=true)
- [x] mi/rutina/rutina-editor.tsx (17 onClick, 16 btn) - COMPLEJO, nucleo de la rutina
- [x] mi/rutina/generar-form.tsx (10) - generacion IA de rutina
- [x] mi/rutina/builder-manual.tsx (6) - armado manual
- [x] mi/rutina/modal-avanzado.tsx (4)
- [x] components/mi/pulpo-asistente-chat.tsx (12) - chat IA (tocado en commits recientes)
- [x] components/rutinas/timer-descanso.tsx (9) - timer + push
- [x] components/rutinas/boton-pedir-ayuda.tsx (8)
- [x] components/rutinas/logro-dia-completado.tsx (5)
- [x] components/rutina/boton-google-calendar.tsx (6) - export ICS /api/rutina/ics
- [x] components/rutina/explicacion-modal.tsx (4)
- [x] components/rutina/publicar-plantilla.tsx (3) - RIESGO: 4 <button> dentro de <form>
- [x] components/rutina/codigo-entrenador.tsx (1) - RIESGO: <button> sin type= en <form>
- [x] components/monetizacion/equipamiento-sugerido.tsx (1)
- [x] panel/ajustes/credenciales-individual-form.tsx - alta de credenciales individuales

## Cacería de Bugs Backend

Sweep del 2026-09-12. **CERRADA: P0, P1 y P2 en 0. `tsc --noEmit` en verde.**

### Resultados de los 4 sweeps
- **Fugas de memoria: LIMPIO.** Los 8 `setInterval` y todos los `addEventListener` en `src/` tienen cleanup pareado.
- **Fugas de auth: LIMPIO.** `admin/actions.ts` 16/16 con `requireSuperadmin()`. Los actions sin guard son públicos por diseño. `impersonar-actions.ts` delega en `lib/impersonation.ts`, que valida superadmin.
- **N+1: 6 infractores → 6 resueltos.**
- **Mutaciones silenciosas: 12 de riesgo real → 12 resueltas.**

### Infraestructura nueva
`src/lib/db/mutaciones.ts` — `aplicarMutacion()` (lanza) e `intentarMutacion()` (devuelve `{ok,msg}`). Ambas exigen `.select("id")` en la mutación y detectan el caso `error: null` + 0 filas. Mensaje canónico: `MSG_CERO_FILAS = "Operación rechazada por la base de datos (0 filas afectadas)"`.

### P0 — mutaciones silenciosas críticas (12/12)
- [x] `mi/pagos/actions.ts:105` comprobante_ref — log + registrarError, no corta el checkout
- [x] `mi/pagos/actions.ts:116` estado="rechazado" — log + registrarError
- [x] `mi/pagos/actions.ts:193` guardar email — devuelve error al socio
- [x] `mi/pagos/actions.ts:211` mp_preapproval_id — corta con error (riesgo de doble débito)
- [x] `mercadopago-connect.ts:187` guardarTokens — `aplicarMutacion` (lanza)
- [x] `mercadopago-connect.ts:204` desvincular — `aplicarMutacion` (lanza)
- [x] `cobro-socio.ts:187` rechazarPagoSocio — ahora devuelve `{ok,msg}`; el webhook lo loguea
- [x] `panel/planes/actions.ts:147` alternarPlan — + scope `gimnasio_id` (faltaba) + verificación
- [x] `panel/planes/actions.ts:156` eliminarPlan — verificación de filas
- [x] `configurar-pin/actions.ts:97` y `:154` — best-effort preservado, fallo al log
- [x] `cambiar-clave/actions.ts:31` — devuelve error (evitaba loop infinito de cambio de clave)
- [x] `reset-clave/page.tsx:68` — idem, inline (client component, no puede importar server-only)

### P1 — N+1 (6/6)
- [x] `cron/cuotas/route.ts` — push agrupados por (días, vencimiento) vía `enviarPush(ids[])`, UPDATEs de morosidad/abandono en batch con `.in()`. ~3000 round-trips → ~10.
- [x] `cron/asistente-ia/route.ts` — 3 consultas globales pre-loop + agrupación en memoria. **Además arregló 2 bugs de datos:** las consultas de actividad no filtraban por gimnasio (escaneaban toda la plataforma por gym) y `checkins_mes` del resumen mensual contaba los check-ins de TODOS los gimnasios como propios.
- [x] `webhooks/mercadopago/route.ts:127` — búsqueda por cuenta en tandas paralelas de 5 con corte temprano
- [x] `admin/partner/actions.ts:338` — un DELETE con `metadata->>gimnasio_id in (...)`
- [x] `admin/actions.ts:800` — Auth API en tandas de 10 + un solo UPDATE `.in()` de profiles
- [x] `admin/actions.ts:959` — deleteUser en tandas de 10 (la Auth API no tiene batch)

### P2 — cascadas y rollbacks (4/4)
- [x] `panel/clientes/actions.ts:633-635` — deletes secundarios tolerantes pero logueados
- [x] `panel/clientes/actions.ts:176` — rollback de profile huérfano verificado
- [x] `admin/actions.ts:317` — delete de registros_entrada con error chequeado
- [x] `admin/actions.ts:597` — rollback de gimnasio verificado; si falla, el mensaje avisa que quedó huérfano

## Operación Limpieza — Código Muerto (COMPLETADA, tsc verde en cada lote)

Método: `ts-prune` + grep exacto `-w` cruzado en `src/` para descartar falsos positivos (barrels/type-only), luego borrado + `npx tsc --noEmit` cada 5 cambios.

### Dependencias
- [x] `npm uninstall lottie-react` — única de las 11 flaggeadas por `unimported` con 0 imports reales confirmados (`jspdf-autotable` y `qrcode` son imports dinámicos, falsos positivos; `lucide-react` con 89 usos confirma que `unimported` está mal configurado para App Router — sus 364 "archivos huérfanos" no son confiables, se ignoró esa sección del reporte).

### Archivos completos borrados (huérfanos 100% confirmados)
- [x] `src/components/progreso/mini-registro-progreso.tsx`
- [x] `src/lib/partners/notificaciones.ts`
- [x] `src/app/panel/ajustes/tema-preview.tsx` (reemplazado por `tema-preview-completo.tsx`)

### Exports/tipos podados quirúrgicamente (0 usos en todo el repo)
- [x] `src/lib/uploadthing.ts` — `UploadButton`, `UploadDropzone`, `uploadFiles` (se conservó `useUploadThing`, sí usado)
- [x] `src/lib/frases-motivadoras.ts` — `obtenerFraseDelDia`
- [x] `src/lib/tema.ts:541` — `COLORES_POR_ESTILO`
- [x] `src/types/partner.ts` — `PartnerMilestoneAward`, `CupoGratuitoInfo`, `ResultadoOperacionConLimite`
- [x] `src/app/login/actions.ts:256` — `loginDevAction` (login hardcodeado con password en texto plano, doble motivo para sacarlo)
- [x] `src/lib/img/comprimir.ts:10` — `OpcionesCompresion`
- [x] `src/lib/logros/actions.ts:274` — `contarReaccionesLogro`
- [x] `src/lib/logros/tipos.ts:20` — `PERDON_DIAS` (verificado: `calcularRacha()` usa lógica de huecos, no esta constante — no era un bug, era un remanente de un diseño anterior)
- [x] `src/lib/offline/cola.ts:78` — `leerCola`
- [x] `src/lib/plataforma/planes.ts:4` — `PlanPlataforma`
- [x] `src/lib/rutina/dropset-actions.ts:116` — `obtenerUltimoDropSetCliente` (+ import `RegistroDropSet` que quedó huérfano tras el borrado)

Grupo B (exports "(used in module)", solo redundantes dentro de su propio archivo) — **ignorado por decisión explícita**, ROI demasiado bajo.

### Callejones sin salida (NO se tocaron — lógica de negocio/dominio, no basura incidental)
- `src/lib/rutina/recuperacion.ts:365` `verificarRecuperacion48h` — 0 usos, pero implementa la regla de recuperación 48h del motor científico (prioridad `science-workout-engine`). Puede ser feature sin cablear, no dead code. Requiere decisión del dueño: ¿cablear en `motor.ts`/`validarYRepararPlan` o borrar?
- `src/lib/rutina/tipos.ts:265,326` `VOLUMEN_PARAMS`, `OPCIONES_AVANZADAS_DEFAULT` — config del motor de rutinas, mismo caso.
- `src/app/panel/planes/actions.ts:107` `asegurarPlanDefecto` — server action de negocio (crea plan mensual por defecto), probablemente pensada para onboarding y no cableada.
- `src/app/mi/pagos/actions.ts:28` `iniciarPagoMercadoPago` — flujo de pago completo (MercadoPago), 0 usos pero es lógica de pagos real, no se borra sin confirmación explícita.

Ningún `tsc` fallo durante todo el proceso — no hubo falsos positivos reales en los borrados aplicados.

## Suite de Tests (Playwright) — Misión 2

Setup: `@playwright/test` instalado a mano (el wizard `npm init playwright@latest` es
interactivo y no respeta `--yes` en este entorno) + `npx playwright install chromium`.
Config en `playwright.config.ts` (baseURL http://localhost:3000, 1 worker, trace/screenshot
on failure). Credenciales de prueba en `login-prueba-browser` (memoria) / `credenciales-locales.md`.

3 flujos críticos elegidos (login es prerequisito de los otros 2, no cuenta como el "3"):
1. `tests/login.spec.ts` — login cliente gym (`migym/99999999/test1234`) y login dueño
   (`migym/30111222/gym1222`); credenciales invalidas muestran error y no redirigen.
   Es el flujo más usado y bloqueante: si se rompe, nada más funciona.
2. `tests/cobro-socio.spec.ts` — dueño registra un pago manual a un socio desde
   `panel/clientes/[id]/registrar-pago-modal.tsx` y verifica que el estado de cuota
   cambia en la UI (flujo de dinero, tocado en el sweep de mutaciones silenciosas).
3. `tests/generar-rutina.spec.ts` — cliente particular genera una rutina IA
   (`mi/rutina/generar-form.tsx`) y verifica que aparece el editor con al menos un
   ejercicio (motor científico, componente más complejo del panel cliente).

Orden estricto: escribir test 1 → verde → test 2 → verde → test 3 → verde.
Server dev debe estar corriendo en :3000 antes de correr los tests (`npm run dev`).

### [x] Test 1: tests/login.spec.ts — VERDE (3/3)
- `credenciales-locales.md` estaba STALE: el gym "migym" ya no existe en la BD
  dev (se verificó con query directa a `gimnasios`); los gyms reales hoy son
  wellness/ironpulse/energygym/genesisgym/sante. Las claves de "sante" del .md
  tampoco andaban (cambiaron desde que se documentaron).
- Corrida `node scripts/reset-single-gym.mjs` (con permiso explícito del dueño,
  es DESTRUCTIVO: resetea la BD dev a un único gym "sante", pisa wellness/
  ironpulse/energygym/genesisgym). Las credenciales que generó son IDÉNTICAS
  a las que ya estaban en el .md → confirmado que el .md en sí era correcto
  para "sante", el problema real era que la sesión de prueba usaba "migym".
- Hallazgo no obvio: el dueño de prueba de sante (DNI 12345678) reutiliza el
  mismo `auth.users.id` que `SUPERADMIN_ID` en `.env.local` → `login/actions.ts`
  lo redirige a `/admin`, NO a `/panel`. Es así por diseño del script de reset,
  no un bug. El test acepta `/panel` o `/admin`.
- Selectores usados: `getByRole("button", {name: "Con DNI (Gimnasio)"})` para
  cambiar de modo, `input[name="gimnasio"|"dni"|"clave"]`, botón "Entrar".
  Para el mensaje de error hubo que `.filter({hasText})` sobre `getByRole("alert")`
  porque Next.js también renderiza un `role="alert"` invisible
  (`#__next-route-announcer__`) que rompía el matcher en "strict mode".

### [x] Test 2: tests/cobro-socio.spec.ts — VERDE (1/1)
- Confirmado el "happy path" del cobro manual (`registrarPago` en
  `panel/clientes/actions.ts`) intacto después de los fixes de IDOR/N+1 de hoy:
  el dueño entra, abre el modal, confirma con los defaults (plan actual +
  monto vacío = precio de lista) y ve `"Pago registrado. Cuota al día hasta…"`.
- 3 obstáculos de entorno, ninguno del flujo de negocio en sí:
  1. El dueño de prueba (== SUPERADMIN_ID) aterriza en `/admin`, que resultó
     ser un **Cockpit Dev** con un switcher de 1 click ("Entrar como Dueño
     (Ir a /panel)") — mejor que un `goto` directo, que colgaba: la conexión
     HMR de `next dev` deja la red activa para siempre y `networkidle` nunca
     resuelve. Lección para los próximos tests: evitar `waitForLoadState("networkidle")`
     en este repo.
  2. `onboarding-dueno.tsx` (tutorial de 5 pasos) tapa la pantalla en el primer
     ingreso a `/panel/clientes` con un overlay que intercepta clicks; hay que
     cerrarlo con el botón "Saltear" (guarda `onboarding_dueno_oculto` en
     localStorage) antes de interactuar con la lista.
  3. Con el modal de pago abierto, hay DOS botones "Registrar pago" en el DOM
     a la vez (el trigger + el submit del form) → hace falta `.first()` o
     scoping por el `dialog`.
- Selectores: `getByRole("link", {name: /Lucas Socio/})` para entrar al
  detalle, `getByRole("dialog", {name: "Registrar pago"})` para scopear el
  modal, texto `/Pago registrado\. Cuota al día hasta/` como assertion final.

### [x] Test 3: tests/generar-rutina.spec.ts — VERDE (1/1) a la primera
- Flujo real: generación de rutina **desde el panel del dueño** (no desde
  `/mi` del cliente) vía `RutinaPanelDueno` → `GenerarRutinaForm` →
  `generarRutinaCliente` en `panel/clientes/actions.ts` (motor científico).
- Sin sorpresas nuevas de entorno: reutilizó 1:1 el helper de login+switcher
  `/admin`→`/panel` y el bypass de "Saltear" ya resueltos en el Test 2.
- El bloque de rutina es un `<details>` que puede empezar cerrado si el socio
  ya tiene una rutina previa (el reset script le crea una) → el test lo abre
  con `summary.click()` solo si no está `open`, y matchea el botón por
  `/Generar rutina|Regenerar rutina/` (el label cambia según `tieneRutina`).
  Assertion final: texto exacto `"Rutina generada para el cliente."`.

## CIERRE MISIÓN 2 — Suite E2E Playwright

**5/5 tests verdes** corriendo la suite completa (`npx playwright test --project=chromium`,
1 worker, ~29s). Los 3 flujos críticos del negocio quedan blindados:
1. Login (cliente + dueño + credenciales inválidas) — `tests/login.spec.ts` (3 tests)
2. Cobro manual de cuota — `tests/cobro-socio.spec.ts` (1 test)
3. Generación de rutina del motor científico desde el panel del dueño —
   `tests/generar-rutina.spec.ts` (1 test)

**Hallazgo de datos más importante de toda la misión:** `credenciales-locales.md`
tenía un gimnasio de prueba ("migym") que ya no existe en la BD dev. El gym
vigente hoy es **"sante"**, mantenido por `node scripts/reset-single-gym.mjs`
(DESTRUCTIVO — pisa wellness/ironpulse/energygym/genesisgym, pedir permiso
antes de correrlo). Ya actualizada la memoria `login-prueba-browser` con esto.

**Deuda/limitaciones conocidas de la suite (no bloqueantes, para la próxima sesión):**
- No cubre el webhook de MercadoPago (`/api/pagos-socio/webhook`,
  `/api/webhooks/mercadopago`) ni el cron de cuotas — son server-to-server,
  no UI-driven, quedan fuera del alcance de Playwright E2E tal como está armado.
- `playwright.config.ts` no tiene `webServer` configurado: asume que el dev
  server ya está corriendo en :3000 (correcto para esta sesión interactiva,
  pero un CI necesitaría agregarlo).
- Todos los tests dependen de que el gym "sante" y sus 2 usuarios de prueba
  sigan existiendo con las claves de `credenciales-locales.md`. Si se vuelve a
  romper, correr `reset-single-gym.mjs` (con permiso) antes de re-correr la suite.

- FASE UI/UX squircles+haptics: registro-partner/page.tsx, admin/vista-switcher.tsx (ahora client, hapticoImpactoMedio en 3 forms + link), panel/onboarding-dueno.tsx, panel/clientes/cliente-row.tsx, panel/caja/modal-abrir-caja.tsx y modal-cerrar-caja.tsx: rounded-md/lg -> rounded-[10/12/16px], tabular-nums font-mono en montos/DNI/dias, h-11 en CTA onboarding, hapticoImpactoMedio en submits de caja. tsc --noEmit OK.

## Feature: Medidor de Aforo en Tiempo Real (Fase 1)
- Componente: `src/components/mi/barra-aforo-animada.tsx` (Liquid Glass, tramos verde/ámbar/carmesí, popover con hapticoImpactoSuave).
- Lógica: `src/lib/aforo/actions.ts` -> obtenerAforo(gimnasioId): cuenta `registros_entrada` de los últimos 90 min (ventana de sesión, no hay check-out real) / `gimnasios.capacidad_maxima` (columna nueva, default 50, migración `add_capacidad_maxima_gimnasios`).
- Montado en `/mi` (arriba, antes de PulpoRetencionCard) y en header de `/mi/rutina`. Oculto para cuentas individuales (`tipo_cuenta=individual`).
- Pendiente: UI en /panel/ajustes para editar `capacidad_maxima` por gym (hoy solo vía SQL).

## Fase 2: Config Capacidad Máxima de Aforo (/panel/ajustes)
- `src/app/panel/ajustes/capacidad-aforo-form.tsx`: stepper +/- táctil (h-11, hapticoImpactoSuave/hapticoExito), tabular-nums font-mono.
- Action `actualizarCapacidadMaxima` en `src/app/panel/ajustes/actions.ts` (valida 1-2000, revalida /panel/ajustes y /mi).
- Montado como nueva sección modal "Aforo del gimnasio" en `src/app/panel/ajustes/page.tsx`.
