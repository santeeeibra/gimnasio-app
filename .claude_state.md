## Zero-waste: revalidatePath faltante (sweep 2026-09-15)

### P0 — plata / pantallas que ya se sabe que quedan stale

- [x] `src/app/panel/clientes/actions.ts:318-320` — `registrarPagoInterno`. Agregado `revalidatePath("/panel/ingresos")` y `revalidatePath("/panel")`.

### P1 — rendimiento/UX en pantallas de uso frecuente

- [x] `src/app/checkin/actions.ts` — `marcarIngreso`. Agregado `revalidatePath("/panel/asistencia")` + `revalidatePath("/panel")` justo después del insert en `registros_entrada`.
- [x] `src/app/panel/ajustes/staff-actions.ts` — falso positivo del sweep (el glob `**/actions.ts` no matcheaba `staff-actions.ts`). Las tres funciones ya revalidan `/panel/ajustes`. Sin cambios.

### P2 — revisado, sin bug

- [x] `src/app/panel/asistencia/actions.ts:99,125` — no existe página `/panel/asistencia` (solo `actions.ts`). El widget que consume `obtenerPedidosActivos()` (`widget-asistencia-sala.tsx`) vive en `/panel/page.tsx` y se autorefresca por polling client-side, no por `revalidatePath`. El `revalidatePath("/panel")` ya presente es correcto. Sin cambios.

### Sweeps que dieron limpios (no tocar)

- `panel/planes`, `panel/mensajes`, `panel/buzon`, `mi/*`, `admin/*`, `panel/caja`: cada mutación revalida su propia pantalla y, cuando corresponde, la pantalla hermana (dashboard/caja). Sin gaps evidentes.
